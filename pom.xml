<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.hb.dokkan</groupId>
    <artifactId>dokkan-web</artifactId>
    <packaging>pom</packaging>
    <version>1.0.0-SNAPSHOT</version>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <java.version>21</java.version>
        <spring.version>3.2.5</spring.version>
        <org.mapstruct.version>1.4.1.Final</org.mapstruct.version>
        <mybatis-plus.version>3.5.10.1</mybatis-plus.version>
        <fastjson.version>1.2.71</fastjson.version>
        <hikaricp.version>6.2.1</hikaricp.version>
        <dokkan.version>1.0.0-SNAPSHOT</dokkan.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!--内部依赖 start-->
            <dependency>
                <groupId>com.hb.dokkan</groupId>
                <artifactId>dokkan-common</artifactId>
                <version>${dokkan.version}</version>
            </dependency>
            <dependency>
                <groupId>com.hb.dokkan</groupId>
                <artifactId>dokkan-infrastructure</artifactId>
                <version>${dokkan.version}</version>
            </dependency>
            <dependency>
                <groupId>com.hb.dokkan</groupId>
                <artifactId>dokkan-service</artifactId>
                <version>${dokkan.version}</version>
            </dependency>
            <!--内部依赖 end-->

            <!-- HikariCP连接池-->
            <dependency>
                <groupId>com.zaxxer</groupId>
                <artifactId>HikariCP</artifactId>
                <version>${hikaricp.version}</version>
            </dependency>

            <!--springboot管理-->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!--fastjson start-->
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>fastjson</artifactId>
                <version>${fastjson.version}</version>
            </dependency>
            <!--fastjson end-->

            <!--mybatis start-->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-boot-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <!--mybatis end-->

            <!--转换类 start-->
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${org.mapstruct.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct-processor</artifactId>
                <version>${org.mapstruct.version}</version>
            </dependency>
            <!--转换类 end-->
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.1</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>${project.build.sourceEncoding}</encoding>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <modules>
        <module>dokkan-common</module>
        <module>dokkan-api</module>
        <module>dokkan-config</module>
        <module>dokkan-starter</module>
        <module>dokkan-service</module>
        <module>dokkan-infrastructure</module>
    </modules>


</project>